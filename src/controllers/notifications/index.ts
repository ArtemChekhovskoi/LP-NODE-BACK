import getUserNotifications from "@controllers/notifications/getUserNotifications";
import postUpdateNotificationStatus from "@controllers/notifications/postUpdateNotificationStatus";

const notifications = {
  getUserNotifications,
  postUpdateNotificationStatus,
};

export default notifications;
